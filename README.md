# til

Today I Learned — catatan singkat harian: snippet, gotcha, dan hal yang baru kepakai di project.

- `til/` — satu file per catatan
- `log/` — worklog per bulan

## konvensi

- `til/YYYY-MM-DD-slug.md` — satu catatan per file, judul = topik
- `log/YYYY-MM.md` — worklog per bulan, satu baris per entri
