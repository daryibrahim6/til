# flex min-w-0

Flex/grid child min-width:auto — `min-w-0` wajib biar bisa menyusut.

```ts
// dipakai
const w = Math.floor((P+g)/(d+g))
```

> tambahan — Flex/grid child min-width:auto — `min-w-0` wajib biar bisa menyusut.

> tambahan — Flex/grid child min-width:auto — `min-w-0` wajib biar bisa menyusut.
