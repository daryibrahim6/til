# inset shorthand

`inset:0` = top/right/bottom/left sekaligus.

```
// catatan cepat
const x = arr.at(-1)
```
# inset shorthand

`inset:0` = top/right/bottom/left sekaligus.

```css
.component { min-width: 0; }
```

> update 9
