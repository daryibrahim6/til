# key prop = reset

`key={id}` ganti = remount total — cara bersih reset state child.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```

> tambahan: fix — 03 Feb

> tambahan: fix — 08 Feb
