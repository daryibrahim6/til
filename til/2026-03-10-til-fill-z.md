# object-contain

Foto mesin aspect beda → `object-contain` + bg texture — rapi tanpa crop.

```ts
// dipakai
const w = Math.floor((P+g)/(d+g))
```

> tambahan — Foto mesin aspect beda → `object-contain` + bg texture — rapi tanpa crop.
