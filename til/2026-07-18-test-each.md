# it.each

Parametrize case — satu test banyak input.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```

> tambahan: tambah — 18 Jul

> tambahan: tambah — 18 Jul
