# token CSS var

`--brand` di :root — theme ganti satu tempat.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```

> tambahan: tambah — 05 Aug

> tambahan: kerjain — 11 Aug
