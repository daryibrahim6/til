# font subset

`subsets:['latin']` — pangkas ukuran font file.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```
# font subset

`subsets:['latin']` — pangkas ukuran font file.

```css
.component { min-width: 0; }
```

> update 7
