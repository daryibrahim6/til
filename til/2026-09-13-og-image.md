# opengraph-image

`opengraph-image.png` di route — og:image otomatis.

```
GET /api/orders -> 401 tanpa kredensial
```
# opengraph-image

`opengraph-image.png` di route — og:image otomatis.

```css
.component { min-width: 0; }
```

> update 7
# opengraph-image

`opengraph-image.png` di route — og:image otomatis.

```
GET /api/orders -> 401 tanpa kredensial
```

> update 8
