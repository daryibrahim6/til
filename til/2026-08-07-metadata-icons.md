# Next file-convention icons

`src/app/favicon.ico` + `icon.png` + `apple-icon.png` = head tags otomatis. Convention ini MENANG atas metadata.icons manual — sempat ketuker.
# metadata icons Next

`icon.png`/`apple-icon.png`/`favicon.ico` di app/ auto ke-detect.

```
GET /api/orders -> 401 tanpa kredensial
```

> update 5
