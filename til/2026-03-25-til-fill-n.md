# pill nav

Floating pill navbar: `fixed` + `backdrop-blur` + rounded-full — konten butuh padding-top offset.

```css
.tile{min-width:0}
```

> tambahan — Floating pill navbar: `fixed` + `backdrop-blur` + rounded-full — konten butuh padding-top offset.
