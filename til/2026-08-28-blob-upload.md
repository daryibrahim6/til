# upload blob

Validasi MIME+size di server, bukan nama file dari client.

```
// catatan cepat
const x = arr.at(-1)
```
# upload blob

Validasi MIME+size di server, bukan nama file dari client.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```

> update 4
