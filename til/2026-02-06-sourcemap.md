# source map prod

Matikan di prod (`productionBrowserSourceMaps:false`) — kecuali butuh debug.

```css
.component { min-width: 0; }
```
# source map prod

Matikan di prod (`productionBrowserSourceMaps:false`) — kecuali butuh debug.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```

> update 2
