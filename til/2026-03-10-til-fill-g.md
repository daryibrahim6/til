# aria-pressed

Toggle state visual + `aria-pressed` — jangan andalin :focus-visible.

```css
.tile{min-width:0}
```

> tambahan — Toggle state visual + `aria-pressed` — jangan andalin :focus-visible.
