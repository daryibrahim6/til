# cookies() async

`await cookies()` di server component — breaking change.

```css
.component { min-width: 0; }
```
# cookies() async

`await cookies()` di server component — breaking change.

```
// catatan cepat
const x = arr.at(-1)
```

> update 4
