# :has() — parent selector akhirnya ada

```css
card:has(img) { padding: 0 }
form:has(:invalid) .submit { opacity: .5 }
```
Udah support semua browser modern — banyak hack JS bisa dibuang.

> tambahan: tambah — 02 Feb
