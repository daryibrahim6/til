# rate limit sliding window di API route

@upstash/ratelimit `slidingWindow(n, '1 m')` per IP — 4 baris kode, endpoint generate/upload aman dari abuse.
# rate limit Upstash

Fixed window per IP di Redis — `INCR`+`EXPIRE` simpelnya.

```css
.component { min-width: 0; }
```

> update 4
