# params await

`params` juga Promise — `const {id} = await params`.

```css
.component { min-width: 0; }
```
# params await

`params` juga Promise — `const {id} = await params`.

```css
.component { min-width: 0; }
```

> update 5
