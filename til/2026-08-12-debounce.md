# debounce pattern

`clearTimeout`+`setTimeout` — tahan call sampai user berhenti ngetik.

```
// catatan cepat
const x = arr.at(-1)
```
# debounce pattern

`clearTimeout`+`setTimeout` — tahan call sampai user berhenti ngetik.

```css
.component { min-width: 0; }
```

> update 7
