# key prop = reset state

`key={item.id}` di detail panel — ganti item otomatis reset state lokal. Gak perlu useEffect cleanup manual.

> tambahan: kerjain — 02 Aug

> tambahan: polish — 17 Aug

> tambahan: fix — 30 Aug
