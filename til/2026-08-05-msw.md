# msw network mock

Intercept fetch di test — response deterministic.

```
GET /api/orders -> 401 tanpa kredensial
```
# msw network mock

Intercept fetch di test — response deterministic.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```

> update 4
