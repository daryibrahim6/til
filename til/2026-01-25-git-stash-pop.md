# git stash pop vs apply

`pop` = apply + drop. Kalau conflict, stash-nya tetap disimpan (aman). `apply` tetap keep stash di list — berguna pas mau apply ke banyak branch.

> tambahan: tambah — 11 Jan
