# -u flag

`push -u origin x` — track remote sekali, `git push` polos selanjutnya.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```

> tambahan: polish — 12 Jul
