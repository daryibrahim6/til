# sharp resize

Downscale upload gede client-side sebelum canvas — hemat memori Konva.

```
GET /api/x -> 401 tanpa env
```

> tambahan — Downscale upload gede client-side sebelum canvas — hemat memori Konva.
