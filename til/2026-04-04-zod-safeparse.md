# safeParse vs parse

`parse` throw; `safeParse` return `{success, data|error}` — di API route safeParse + early return 400 = validasi bersih tanpa try/catch.

> tambahan: tambah — 04 Apr

> tambahan: tune — 05 Apr
