# bundle analyze

`@next/bundle-analyzer` — lihat apa yang bikin js gemuk.

```
GET /api/orders -> 401 tanpa kredensial
```
# bundle analyze

`@next/bundle-analyzer` — lihat apa yang bikin js gemuk.

```
// catatan cepat
const x = arr.at(-1)
```

> update 4
