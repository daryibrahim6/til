# git blame -L

`blame -L10,20` — siapa nulis baris ini.

```
// catatan cepat
const x = arr.at(-1)
```
# git blame -L

`blame -L10,20` — siapa nulis baris ini.

```css
.component { min-width: 0; }
```

> update 2
