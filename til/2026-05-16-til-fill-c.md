# alpha centroid

`np.argwhere(alpha>10)` mean per-axis — ukur optical center gambar transparan.

```
GET /api/x -> 401 tanpa env
```

> tambahan — `np.argwhere(alpha>10)` mean per-axis — ukur optical center gambar transparan.
