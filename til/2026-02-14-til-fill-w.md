# dot texture

`radial-gradient` 1px + background-size — dot pattern tanpa file gambar.

```
GET /api/x -> 401 tanpa env
```

> tambahan — `radial-gradient` 1px + background-size — dot pattern tanpa file gambar.
