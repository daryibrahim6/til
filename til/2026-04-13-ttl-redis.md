# TTL data sementara

`EX` di SET — pending order auto-bersih, terminal permanen.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```
# TTL data sementara

`EX` di SET — pending order auto-bersih, terminal permanen.

```
// catatan cepat
const x = arr.at(-1)
```

> update 3
