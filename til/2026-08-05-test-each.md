# it.each

Parametrize case — satu test banyak input.

```
GET /api/orders -> 401 tanpa kredensial
```
# it.each

Parametrize case — satu test banyak input.

```
// catatan cepat
const x = arr.at(-1)
```

> update 5
