# inset shorthand

`inset:0` = top/right/bottom/left sekaligus.

```
// catatan cepat
const x = arr.at(-1)
```
# inset shorthand

`inset:0` = top/right/bottom/left sekaligus.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```

> update 7
