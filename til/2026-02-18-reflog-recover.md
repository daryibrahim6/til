# reflog — undo apapun

`git reflog` nyimpen semua HEAD movement. Commit 'hilang' hampir selalu bisa di-recover.

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```
