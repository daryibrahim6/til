# aspect tile

`aspect-[4/5]` grid tile — poster/photo seragam, hover overlay rapi.

```
GET /api/x -> 401 tanpa env
```

> tambahan — `aspect-[4/5]` grid tile — poster/photo seragam, hover overlay rapi.
