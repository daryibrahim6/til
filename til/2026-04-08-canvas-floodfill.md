# remove-bg via flood fill dari edge

Flood-fill dari border (bukan threshold global) — putih di DALAM logo ikut kebawa kalau pakai threshold. Queue BFS + tolerance ~30 cukup.

> tambahan: benerin — 07 Apr
