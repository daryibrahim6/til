# proxy/middleware

Auth check di proxy.ts — tapi re-check di handler juga (defense).

```ts
// dipakai di project
export const cn = (...c) => c.filter(Boolean).join(' ')
```
# proxy/middleware

Auth check di proxy.ts — tapi re-check di handler juga (defense).

```
GET /api/orders -> 401 tanpa kredensial
```

> update 5
